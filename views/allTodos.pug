extends layout

block layout-content
    - var deletePath = '/tasks/delete/'
    - var isEditable = true
    div.View.all-todos
        div.content
            div.todos-view
                div.todo-list(style={'max-height': '80vh'})                
                    include todoList.pug 